(function ($, Drupal) {

    'use strict';
  
    Drupal.behaviors.slideshow = {
      attach: function (context, settings) {
        console.log('slideshow script loaded!');
      }
    };
  
  } (jQuery, Drupal));
  